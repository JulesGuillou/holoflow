name: C++ CI with Bazel

on: [push, pull_request]

jobs:
    build:
        runs-on: self-hosted
        steps:
        - uses: actions/checkout@v2
        - name: Build
          run: bazel build //...
        - name: Test
          run: bazel test //...
